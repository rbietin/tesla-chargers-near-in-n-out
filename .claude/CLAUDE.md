# Claude Instructions

Python project using `venv` for virtual environment.

## Commands

```bash
source venv/bin/activate  # Activate venv
python run.py             # Run full pipeline
```

## Key Files

- `src/find_nearby.py` - Contains `MAX_DISTANCE_METERS` config (default 400m)
- `data/` - Generated output, gitignored
